#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../.ncp@0.6.0/bin/ncp" "$@"
  ret=$?
else 
  node  "$basedir/../../../.ncp@0.6.0/bin/ncp" "$@"
  ret=$?
fi
exit $ret
